#include <stdio.h>
int main()
{
    int num;
    printf("Number of pizzas\n");
    scanf("%d", &num);
    if(num>1)
    {
        printf("Check our multiple pizza deals");
    }
    else
    printf("Your total is $8");
}
